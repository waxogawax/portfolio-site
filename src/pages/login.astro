---
import { SignIn } from '@clerk/astro/components';
import BaseLayout from '../layouts/BaseLayout.astro';

const redirectParam = Astro.url.searchParams.get('redirect');
const isSafeRedirect = !!redirectParam && redirectParam.startsWith('/') && !redirectParam.startsWith('//');
const redirectTarget = isSafeRedirect ? redirectParam : '/diary/';

const { userId } = Astro.locals.auth();
if (userId) {
  return Astro.redirect(redirectTarget);
}

const reopenUrl = `/login?redirect=${encodeURIComponent(redirectTarget)}`;
---

<BaseLayout title="Login" description="ログイン">
  <div class="login-page">
    <div class="login-message">
      <h1>ログインが必要です</h1>
      <p>この記事はログインユーザー限定で公開しています。</p>
      <p>初めての方は、メールアドレスを登録してアカウントを作成してください。登録済みの方は、メールアドレスでログインしてください。</p>
    </div>
    <SignIn forceRedirectUrl={redirectTarget} />

    <p class="login-fallback">
      ログイン画面が表示されない場合は、
      <a href={reopenUrl} target="_blank" rel="noopener noreferrer">別ウィンドウでログインページを開く</a>
      をお試しください。Safariでは、Cookieやキャッシュの影響で表示されないことがあります。
    </p>
  </div>
</BaseLayout>

<style>
  .login-page {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 32px;
    padding-block: 56px;
  }

  .login-message {
    max-width: 420px;
    text-align: center;
  }

  .login-message h1 {
    font-family: var(--font-serif);
    font-size: 1.5rem;
    font-weight: 500;
    margin-bottom: 12px;
  }

  .login-message p {
    font-size: 0.9rem;
    line-height: 1.7;
    color: var(--color-muted);
  }

  .login-message p + p {
    margin-top: 8px;
  }

  .login-fallback {
    max-width: 420px;
    text-align: center;
    font-size: 0.8rem;
    line-height: 1.6;
    color: var(--color-muted);
  }

  .login-fallback a {
    color: var(--color-text);
    text-decoration: underline;
  }
</style>
