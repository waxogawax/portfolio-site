# Agent Instructions

See `CLAUDE.md` for project rules.

This is an Astro portfolio site.

Important rules:

- `.astro` page files belong in `src/pages/`.
- Markdown content files belong in `src/content/`.
- Do not put `.astro` files in `src/content/`.
- Do not put `.md` files in `src/pages/`.
- Run `npm run build` before finishing implementation tasks.